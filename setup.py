from setuptools import setup

setup(
   name='file',
   version='1.0',
   description='Python simple file and directory wrappers.',
   author='Dean Haines',
   author_email='vbpupil@gmail.com',
   packages=['file'],  #same as name
   install_requires=[], #external packages as dependencies
)
